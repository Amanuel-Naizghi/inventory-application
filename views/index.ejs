<html>
<head>
    <title>Home</title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% if (locals.data) {%>
            <% data.forEach((item) => { %>
              <li>ID: <%= item.id %> <%= item.name %> <%= item.quantity %> <%= item.rating %> <%= item.category %> <%= item.description %> </li>
              <form action="/<%= item.id %>/delete" method="POST" style="display:inline">
                <button type="submit" onclick="return confirm('Are you sure you want to delete the item?');">Delete</button>
              </form>
            <% }); %>
        <% } %>
    </ul>
    <a href="/add">Add new item</a>
</body>
</html>